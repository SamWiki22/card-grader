/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // no-op change to force a fresh Vercel build after fixing the project's Root Directory setting
};

module.exports = nextConfig;
